<script>
  /********************************************************************************************
   * ⚠️  CRITICAL — MANUAL SYNC REQUIRED
   *
   * THIS FILE IS A STATIC ALIAS FOR THE "LATEST" DOCS VERSION.
   *
   * The import path below MUST ALWAYS MATCH the value of `latestVersion`
   * defined in:
   *
   *     /src/lib/stores/docs.js
   *
   * There is NO automatic linkage between this import and `latestVersion`.
   * SvelteKit requires this import to remain STATIC and HARDCODED.
   *
   * IF YOU CHANGE `latestVersion` IN `/lib/stores/docs.js`,
   * YOU MUST ALSO UPDATE THE IMPORT PATH BELOW.
   *
   * Failure to keep these in sync will cause:
   *   - incorrect content served at /docs/latest
   *   - broken navigation
   *   - SEO inconsistencies
   *   - subtle SSR/prerender bugs
   *
   * Example:
   *   latestVersion = 'v1.1.0'
   *   → update this file to:
   *     import Page from "../v1.1.0/+page.svelte";
   *
   ********************************************************************************************/
  import Page from "../v1.0.0/+page.svelte";
</script>

<Page />
